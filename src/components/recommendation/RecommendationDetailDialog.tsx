// Placeholder — full carousel + metadata + tag chips implemented in Slice 2 Task 6.
export function RecommendationDetailDialog(_props: {
  placeId: string | undefined
  open: boolean
  onOpenChange: (open: boolean) => void
}) {
  return null
}
